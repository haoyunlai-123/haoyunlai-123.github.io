```
@Override
public boolean tryLock(long timeout) {
    // 1.获取线程id
    long id = Thread.currentThread().getId();
    // 2.获取锁
    Boolean success = stringRedisTemplate.opsForValue().setIfAbsent(KEY_PREFIX + lockName, id + "", timeout, TimeUnit.SECONDS);
    return Boolean.TRUE.equals(success);
}
```